# Notebook-ready CSV copies

`ingest_data.py` / `fetch_datasets.py` can place **`PreprocessData.csv`** (and upstream **`data.csv`**) here when those entries succeed in **`data/manifest.json`**. Training notebook also checks **`notebooks/PreprocessData.csv`**.

See **`data/README.md`** for the full layout.
