# Data

- manifest.json lists what ingest_data.py downloads into downloads/ (that folder is gitignored).
- sources/<name>/README.md is documentation per source only; large files do not live there.

Heavy optional sources need environment flags; each source README explains them.
